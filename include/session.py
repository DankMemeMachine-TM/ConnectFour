# Initialized when a game object initializes it, this controls data loaded from a save file, the number of rounds playd so far, etc.
player_names = ["DebugA", "DebugB"];
win_ratio = [0, 0];
rounds = 0;